<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
			 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
			 xmlns:custom="clr-namespace:TennisExplorer.CustomElements"
			 x:Class="TennisExplorer.Pages.FavoritesListPage">
	<ContentPage.Content>
		<StackLayout>
			<Label Text="Welcome to Xamarin.Forms!"
				VerticalOptions="CenterAndExpand" 
				HorizontalOptions="CenterAndExpand" />

			<ListView HasUnevenRows="true" ItemsSource="{Binding Favorites}" SelectedItem="{Binding SelectedFavorite, Mode=TwoWay}" Margin="0,20,0,0">

				<ListView.ItemTemplate>
					<DataTemplate>
						<custom:ExtendedViewCell>
							<Label Text="{Binding Name}" />
						</custom:ExtendedViewCell>
					</DataTemplate>
				</ListView.ItemTemplate>
				<!--<ListView.Behaviors>
					<custom:EventToCommandBehavior Converter="{StaticResource ItemTappedConverter}"
					EventName="ItemTapped"
					Command="{Binding NavigateToPageCommand}" />
				</ListView.Behaviors>-->
			</ListView>
		</StackLayout>
	</ContentPage.Content>
</ContentPage>